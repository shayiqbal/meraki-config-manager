{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Meraki Config Manager",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/main_client.py",
      "console": "integratedTerminal",
      "cwd": "${workspaceFolder}",
      "envFile": "${workspaceFolder}/.env"
    }
  ]
}

