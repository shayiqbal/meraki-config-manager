[project]
name = "meraki-config-manager"
version = "1.0.0"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q --disable-warnings"

[tool.coverage.run]
source = ["config", "meraki_client", "rules", "services", "reporting"]

